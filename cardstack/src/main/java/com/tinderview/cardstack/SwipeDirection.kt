package com.tinderview.cardstack

/**
 * Physical swipe axes the deck can commit along.
 * Apps map these to product actions (Right = like, Left = pass, Up = super like).
 */
public enum class SwipeDirection {
    Left,
    Right,
    Up,
}
